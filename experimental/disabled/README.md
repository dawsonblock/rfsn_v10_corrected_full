# Experimental Disabled Files

These files are not part of Main 23 proof.

The production selected-block path is `RFSNTurboQuantKVManager.retrieve_blocks()`.

True arbitrary partial dequantization remains unimplemented.
